import {NgModule} from '@angular/core'
import {BrowserModule} from '@angular/platform-browser'

import {SensordataAppComponent} from './sensordata-app.component'
@NgModule({
    imports: [BrowserModule],
    declarations: [SensordataAppComponent],
    bootstrap: [SensordataAppComponent]
})
export class AppModule{}